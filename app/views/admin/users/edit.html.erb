<div class="flex flex-col justify-center items-center bg-white">
  <% content_for :title, "Editing user" %>

  <h1>Editing user</h1>

  <%= render "form", user: @user %>

  <br>

  <div class="flex gap-4">
    <%= link_to "Show this user", admin_user_path(@user),class:"py-2 px-3 bg-gray-500 rounded-sm text-white transition-all ease-in hover:scale-105" %> 
    <%= link_to "Back to users", admin_users_path,class:"py-2 px-3 bg-gray-500 rounded-sm text-white transition-all ease-in hover:scale-105" %>
  </div>
</div>
